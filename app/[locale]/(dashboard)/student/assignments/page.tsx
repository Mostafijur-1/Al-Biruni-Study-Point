import { RoutePlaceholder } from "@/components/shared/RoutePlaceholder";

export default function StudentAssignmentsPage() {
  return <RoutePlaceholder eyebrow="Student panel" title="CQ Assignments" description="CQ submission list and image upload workflow." />;
}
