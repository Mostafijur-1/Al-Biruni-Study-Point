import { RoutePlaceholder } from "@/components/shared/RoutePlaceholder";

export default function BatchesPage() {
  return (
    <RoutePlaceholder
      eyebrow="Batches"
      title="Current Online and Offline Batches"
      description="Batch schedules, capacity, class mode, and enrollment availability."
    />
  );
}
